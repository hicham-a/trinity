+++ /opt/xcat/share/xcat/netboot/genesis/x86_64/fs/bin/bmcsetup	2015-12-23 15:03:42.560091048 +0100
@@ -99,6 +99,13 @@
 elif [ "$IPMIMFG" == "47488" ]; then
         LOCKEDUSERS=1
 fi
+#
+# hans.then@clustervision.com
+# 
+# We always assume LOCKEDUSERS=1 as that will work on both types of HW.
+# 
+LOCKEDUSERS=1
+ 
 echo -n "Auto detecting LAN channel..."
 
 while [ -z "$LANCHAN" ]; do 
