[Unit]
Description=Dockerized Glance
Requires=keystone.service
After=keystone.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a glance
ExecStop=/usr/bin/docker stop -t 2 glance

[Install]
WantedBy=multi-user.target
